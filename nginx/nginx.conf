http{
    upstream all{
        server web-django:8001;
        server web-django-2:8002;
        ip_hash;
    }

    server{
        listen 8000;
        location / {
            proxy_pass http://all;
        }
    }
}

events { }