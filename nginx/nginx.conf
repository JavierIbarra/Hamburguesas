http{
    upstream all{
        server backend_1:8001;
        server backend_2:8002;
        ip_hash;
    }

    server{
        listen 8000;
        location / {
            proxy_pass http://all;
        }
    }
}

events { }